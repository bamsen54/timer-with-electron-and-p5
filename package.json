{
  "name": "timer-electron",
  "productName": "timer",
  "version": "1.0.0",
  "description": "timer",
  "main": "main-electron.js",
  
  "scripts": {
    "start": "electron .",
    "package": "build",
    "build": "electron-packager --out winx64 --overwrite --platform win32 --appname clientlmcenter . --icon=icon/bird.ico"

  },
  "author": "Simon Toivola",
  "license": "MIT",
  "dependencies": {
    "electron": "^36.1.0",
    "electron-packager": "^17.1.2"
  },
  "build": {
    "appId": "test",
    "files": "!build-assets${/*}"
  },
  "devDependencies": {
    "@electron-forge/cli": "^7.8.0"
  }
}
